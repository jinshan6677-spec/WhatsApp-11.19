/**
 * Constants Module Exports
 */

const { TEMPLATE_TYPES, TEMPLATE_TYPE_LABELS } = require('./templateTypes');
const { SEND_MODES, SEND_MODE_LABELS } = require('./sendModes');
const { TAB_TYPES, TAB_TYPE_LABELS } = require('./tabTypes');
const { VISIBILITY_TYPES, VISIBILITY_TYPE_LABELS } = require('./visibilityTypes');
const LIMITS = require('./limits');

module.exports = {
  TEMPLATE_TYPES,
  TEMPLATE_TYPE_LABELS,
  SEND_MODES,
  SEND_MODE_LABELS,
  TAB_TYPES,
  TAB_TYPE_LABELS,
  VISIBILITY_TYPES,
  VISIBILITY_TYPE_LABELS,
  LIMITS
};

/**
 * Constants Module Exports
 */

const { TEMPLATE_TYPES, TEMPLATE_TYPE_LABELS } = require('./templateTypes');
const { SEND_MODES, SEND_MODE_LABELS } = require('./sendModes');
const LIMITS = require('./limits');

module.exports = {
  TEMPLATE_TYPES,
  TEMPLATE_TYPE_LABELS,
  SEND_MODES,
  SEND_MODE_LABELS,
  LIMITS
};
